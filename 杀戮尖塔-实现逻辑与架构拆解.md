# 杀戮尖塔（Slay the Spire）实现逻辑、维度体系与底层技术拆解

> 本文目标不是复述玩法，而是把《杀戮尖塔》拆成可实现、可建模、可复刻的系统：核心循环、状态机、数据结构、随机生成、数值平衡、内容管线、技术栈与 Mod 生态。
> 说明：文中对底层代码结构的描述是基于公开资料、可观察玩法机制、Mod 生态和常见工程实践的归纳，不代表 Mega Crit 官方源码逐行说明。

---

## 0. 一句话结论

《杀戮尖塔》的本质是：

```text
有限生命值 + 回合制卡牌战斗 + 路线选择 + 随机构筑奖励
= 在不确定输入下持续做风险管理的单人策略系统
```

它最关键的设计不是“卡牌很多”，而是把所有系统都压到几类清晰资源上：

```text
生命值 HP        失败资源，跨房间继承
能量 Energy      单回合行动预算
卡牌 Card        行动选项
抽牌 Draw        选项刷新速度
格挡 Block       临时防御资源
金币 Gold        路线外的选择权
遗物 Relic       被动规则修改器
药水 Potion      一次性战术兜底
地图节点 Node    风险/收益路径选择
```

所以它的实现核心可以抽象成三个层次：

```text
Run 层：整局爬塔、地图、奖励、金币、遗物、血量、种子
Combat 层：回合、抽弃牌、能量、敌人意图、行动队列
Content 层：卡牌、遗物、药水、敌人、事件、数值表
```

---

## 1. 核心循环

### 1.1 宏观循环

```text
选择角色
  ↓
生成随机种子与地图
  ↓
选择路径节点
  ↓
进入房间
  ├── 战斗
  ├── 精英
  ├── 事件
  ├── 商店
  ├── 休息处
  └── 宝箱
  ↓
结算奖励/惩罚
  ↓
更新整局状态
  ↓
进入下一节点
  ↓
击败 Boss / 死亡 / 进入下一幕
```

### 1.2 微观循环：战斗

```text
战斗开始
  ↓
初始化抽牌堆、弃牌堆、消耗堆、敌人、遗物触发
  ↓
玩家回合开始
  ├── 格挡清零
  ├── 能量恢复
  ├── 抽牌
  ├── 回合开始效果触发
  └── 敌人意图可见
  ↓
玩家出牌
  ├── 检查费用
  ├── 检查目标
  ├── 入行动队列
  ├── 结算卡牌效果
  ├── 触发遗物/状态/能力
  └── 卡牌进入弃牌堆或消耗堆
  ↓
玩家结束回合
  ├── 手牌进入弃牌堆
  ├── 结束回合效果触发
  └── 切换敌方回合
  ↓
敌人按意图行动
  ↓
敌人生成下一回合意图
  ↓
胜负判定
```

### 1.3 设计上真正闭环的东西

表面循环是“打怪拿牌”，底层闭环是“用生命换成长”：

```text
路线选择 → 决定风险密度
战斗表现 → 决定 HP 损耗
奖励选择 → 决定未来战斗能力
商店/事件/休息 → 决定资源转化方式
Boss/精英 → 检验构筑是否成型
```

玩家每一步都在问同一个问题：

```text
我现在承受多少风险，能换来多少未来胜率？
```

这就是为什么《杀戮尖塔》的地图选择、拿牌、删牌、升级、回血、打精英都能变成策略问题。

---

## 2. 维度体系

### 2.1 一级维度：胜负维度

| 维度 | 直接问题 | 对应系统 |
|---|---|---|
| 生存 | 我能不能不死？ | HP、格挡、虚弱、药水、休息 |
| 输出 | 我能不能及时杀死敌人？ | 攻击牌、毒、力量、球、姿态 |
| 成长 | 我能不能越打越强？ | 卡牌奖励、遗物、升级、金币、事件 |

这三者不是并列平均的：

```text
生存是底线，输出是通关条件，成长是长期放大器。
```

早期缺输出会被精英惩罚，中期缺防御会被多段攻击惩罚，后期缺成长会被 Boss 的高血量和机制惩罚。

### 2.2 二级维度：资源维度

| 资源 | 生命周期 | 能否跨战斗 | 设计作用 |
|---|---:|---:|---|
| HP | 整局 | 是 | 最终失败资源 |
| 最大 HP | 整局 | 是 | 长线容错 |
| 能量 | 单回合 | 否 | 限制行动数量 |
| 手牌 | 单回合 | 否 | 当前可选动作 |
| 抽牌堆 | 单战斗 | 否 | 未来动作池 |
| 弃牌堆 | 单战斗 | 否 | 循环中的回收池 |
| 消耗堆 | 单战斗 | 否 | 临时删除池 |
| 格挡 | 单回合 | 否，除非特殊规则 | 当前防御预算 |
| 金币 | 整局 | 是 | 商店选择权 |
| 药水 | 整局 | 是 | 一次性战术保险 |
| 遗物 | 整局 | 是 | 被动规则修改 |

关键点：游戏把大多数战斗资源都限制在“单回合”或“单战斗”，只让 HP、卡组、遗物、金币、药水跨战斗保留。这样可以同时保证：

- 每场战斗有明确开始和结束。
- 单场战斗内可爆发，但难以永久滚雪球。
- 整局成长主要来自卡组和遗物，而不是临时数值堆叠。

### 2.3 三级维度：卡牌功能维度

一张牌可按多个轴分类。

| 分类轴 | 典型取值 | 实现用途 |
|---|---|---|
| 卡牌类型 | 攻击、技能、能力、诅咒、状态 | 决定触发器、结算区、可否打出 |
| 职业颜色 | 红、绿、蓝、紫、无色、诅咒、状态 | 决定奖励池 |
| 费用 | 0、1、2、3、X、特殊费用 | 决定能量检查 |
| 目标 | 自身、单敌、全敌、随机敌、无需目标 | 决定 UI 和效果路由 |
| 稀有度 | 初始、普通、罕见、稀有、特殊 | 决定掉落权重 |
| 战斗定位 | 输出、防御、过牌、能量、缩牌、成长、控制 | 决定数值平衡 |
| 关键词 | 消耗、保留、虚无、固有、回响等 | 决定额外规则 |
| 升级方式 | 数值增强、费用降低、关键词变化、目标变化 | 决定升级数据 |

实现时不要只用 `type = Attack/Skill/Power`，还需要额外标签：

```text
CardDefinition
├── id
├── nameKey
├── color
├── type
├── rarity
├── baseCost
├── targetType
├── tags
├── effects
├── upgradedEffects
├── magicNumbers
└── assetRefs
```

### 2.4 四级维度：时间维度

《杀戮尖塔》的很多策略来自“什么时候获得收益”。

| 时间类型 | 示例 | 特征 |
|---|---|---|
| 即时收益 | 打击、耸肩无视 | 当回合立刻生效 |
| 延迟收益 | 毒、黑暗球、能力牌 | 先投入，后回收 |
| 周期收益 | 每回合抽牌、每回合格挡 | 越早使用越赚 |
| 一次性收益 | 药水、消耗牌 | 高强度但不可重复 |
| 永久收益 | 升级、删牌、遗物 | 跨战斗提升 |

这解释了为什么能力牌强但不能无限塞：能力牌首回合往往亏节奏，只有战斗足够长时才回本。

### 2.5 五级维度：风险维度

| 选择 | 短期风险 | 长期收益 |
|---|---:|---:|
| 打精英 | 高 | 遗物、金币、稀有牌概率 |
| 进事件 | 不确定 | 可能删牌、升级、拿遗物 |
| 去商店 | 低 | 精准购买/删牌 |
| 休息回血 | 低 | 失去升级机会 |
| 休息升级 | 中 | 长期战力提升 |
| 跳过卡牌 | 低 | 保持卡组纯度 |
| 拿诅咒换奖励 | 高 | 获得强力资源 |

核心实现点是：风险不能只靠随机数制造，还要通过可见信息让玩家主动选择。

---

## 3. 核心数据模型

### 3.1 整局状态 RunState

```ts
type RunState = {
  seed: number;
  characterId: string;
  ascensionLevel: number;

  act: 1 | 2 | 3 | 4;
  floor: number;
  map: MapGraph;
  currentNodeId: string;

  hp: number;
  maxHp: number;
  gold: number;

  masterDeck: CardInstance[];
  relics: RelicInstance[];
  potions: PotionInstance[];

  rewardRng: RngState;
  mapRng: RngState;
  combatRng: RngState;

  flags: Record<string, unknown>;
};
```

`RunState` 是存档核心，必须可序列化。地图、卡组、遗物、金币、血量、事件标记都属于整局状态。

### 3.2 战斗状态 CombatState

```ts
type CombatState = {
  turn: number;
  phase: CombatPhase;

  player: PlayerCombatant;
  enemies: EnemyCombatant[];

  energy: number;
  maxEnergyThisTurn: number;

  drawPile: CardInstance[];
  hand: CardInstance[];
  discardPile: CardInstance[];
  exhaustPile: CardInstance[];

  actionQueue: GameAction[];
  pendingRewards: Reward[];

  rng: RngState;
  logs: CombatLogEntry[];
};
```

战斗状态不能直接替代整局状态。战斗结束后只把必要结果写回 `RunState`：

```text
HP 变化
金币变化
药水变化
卡牌变化
遗物变化
事件标记变化
```

### 3.3 卡牌定义与卡牌实例

卡牌要区分“定义”和“实例”。

```ts
type CardDefinition = {
  id: string;
  nameKey: string;
  type: CardType;
  color: CardColor;
  rarity: Rarity;
  cost: CostDef;
  target: TargetType;
  tags: CardTag[];
  effects: EffectDef[];
  upgrade: UpgradeDef;
};

type CardInstance = {
  uuid: string;
  definitionId: string;
  upgraded: boolean;
  costForTurn?: number;
  misc?: number;
  temporaryFlags: CardRuntimeFlag[];
};
```

为什么要分开：

- 同名卡可能有多张，需要独立 `uuid`。
- 升级状态属于实例。
- 本回合费用变化属于运行时状态。
- 某些卡有 `misc` 记忆值，例如永久成长类效果。

### 3.4 效果系统 Effect

卡牌不要写成大量硬编码 `if cardId == xxx`。更合理的底层是效果列表：

```ts
type EffectDef =
  | { kind: "damage"; amount: ValueRef; hits?: number }
  | { kind: "block"; amount: ValueRef }
  | { kind: "draw"; amount: ValueRef }
  | { kind: "gainEnergy"; amount: ValueRef }
  | { kind: "applyPower"; powerId: string; amount: ValueRef }
  | { kind: "exhaust"; selector: CardSelector }
  | { kind: "createCard"; cardId: string; destination: PileRef }
  | { kind: "conditional"; condition: ConditionDef; then: EffectDef[]; else?: EffectDef[] };
```

复杂牌可以通过组合效果实现：

```text
耸肩无视 = 获得格挡 + 抽牌
顺劈斩 = 对所有敌人造成伤害
祭品 = 失去生命 + 获得能量 + 抽牌 + 消耗自身
突破极限 = 力量翻倍 + 消耗自身，升级后不消耗
```

实际商业项目中仍会保留“自定义脚本/自定义类”出口，因为总有少数卡牌无法优雅数据化。

### 3.5 行动队列 Action Queue

《杀戮尖塔》这类游戏强依赖行动队列，而不是立即递归调用效果。

```text
玩家打出卡牌
  ↓
创建 UseCardAction
  ↓
UseCardAction 展开为 DamageAction / BlockAction / DrawAction
  ↓
每个 Action 结算前后触发 Relic / Power / Card Hook
  ↓
队列为空后等待下一输入
```

伪代码：

```ts
function updateCombat(state: CombatState) {
  while (state.actionQueue.length > 0) {
    const action = state.actionQueue.shift();
    triggerHooks("beforeAction", action, state);
    action.resolve(state);
    triggerHooks("afterAction", action, state);
    checkDeaths(state);
  }
}
```

行动队列的好处：

- 动画和逻辑可以同步。
- 遗物、能力、状态可以插队。
- 多段伤害、反伤、死亡触发不会互相递归炸栈。
- 方便调试和回放。

---

## 4. 战斗系统实现逻辑

### 4.1 战斗状态机

```text
INIT
  ↓
PRE_BATTLE
  ↓
PLAYER_TURN_START
  ↓
PLAYER_INPUT
  ↓
RESOLVING_ACTIONS
  ↓
PLAYER_TURN_END
  ↓
ENEMY_TURN_START
  ↓
ENEMY_ACTIONS
  ↓
ENEMY_TURN_END
  ↓
CHECK_END
  ├── 胜利 → REWARD
  ├── 死亡 → GAME_OVER
  └── 继续 → PLAYER_TURN_START
```

实现时要把“允许玩家输入”和“正在结算动作”分开，否则容易出现拖牌期间又触发抽牌、敌人死亡后还被选为目标等问题。

### 4.2 出牌流程

```text
点击/拖拽卡牌
  ↓
检查是否在 PLAYER_INPUT 阶段
  ↓
检查费用是否足够
  ↓
检查卡牌是否可打出
  ↓
如果需要目标，进入选目标状态
  ↓
支付费用
  ↓
触发 onUseCard
  ↓
把卡牌效果加入行动队列
  ↓
卡牌移动到弃牌堆/消耗堆/能力区
  ↓
结算行动队列
```

关键不是“卡牌造成多少伤害”，而是这套过程里的 Hook：

| Hook | 用途 |
|---|---|
| `canUseCard` | 禁止打出、费用检查、状态限制 |
| `onUseCard` | 使用牌时触发遗物/能力 |
| `modifyDamage` | 力量、易伤、虚弱、遗物修正 |
| `modifyBlock` | 敏捷、能力、遗物修正 |
| `onCardPlayed` | 连击、弃牌、抽牌触发 |
| `onCardExhausted` | 消耗流触发 |
| `onEnemyDeath` | 死亡奖励和连锁触发 |

### 4.3 伤害结算管线

推荐把伤害拆成多个阶段：

```text
基础伤害
  ↓
攻击者修正：力量、卡牌自身倍率、遗物
  ↓
防御者修正：易伤、无实体、特殊抗性
  ↓
最终伤害取整
  ↓
先扣格挡
  ↓
剩余伤害扣 HP
  ↓
触发受伤/失血/死亡 Hook
```

示意伪代码：

```ts
function calculateDamage(base: number, source: Combatant, target: Combatant, ctx: DamageContext) {
  let value = base;
  value = applySourceModifiers(value, source, ctx);
  value = applyTargetModifiers(value, target, ctx);
  value = Math.max(0, Math.floor(value));
  return value;
}

function dealDamage(target: Combatant, damage: number) {
  const blocked = Math.min(target.block, damage);
  target.block -= blocked;
  const hpLoss = damage - blocked;
  target.hp -= hpLoss;
  triggerHooks("onHpLoss", { target, hpLoss });
}
```

注意：游戏里“受到攻击伤害”和“失去生命”是两类不同事件。前者会被格挡抵消，后者通常绕过格挡。

### 4.4 格挡设计

格挡每回合清零，是整个战斗节奏的关键。

```text
如果格挡可永久累积：
  玩家会倾向于先堆防御，再慢慢输出

如果格挡每回合清零：
  玩家每回合都要重新判断攻击/防御/成长
```

这让每回合都产生决策压力，也让敌人意图系统变得有意义。

### 4.5 抽牌、弃牌、洗牌

标准牌堆循环：

```text
drawPile → hand → discardPile
                 ↘ exhaustPile

当 drawPile 不足：
  1. 抽完 drawPile 剩余牌
  2. 洗 discardPile
  3. 放入 drawPile
  4. 继续抽牌
```

伪代码：

```ts
function drawCards(state: CombatState, count: number) {
  for (let i = 0; i < count; i++) {
    if (state.drawPile.length === 0) {
      if (state.discardPile.length === 0) return;
      state.drawPile = shuffle(state.discardPile, state.rng);
      state.discardPile = [];
    }
    state.hand.push(state.drawPile.pop()!);
  }
}
```

洗牌推荐使用 Fisher-Yates，并且必须由种子驱动：

```ts
function shuffle<T>(items: T[], rng: Rng): T[] {
  const arr = [...items];
  for (let i = arr.length - 1; i > 0; i--) {
    const j = rng.int(0, i);
    [arr[i], arr[j]] = [arr[j], arr[i]];
  }
  return arr;
}
```

### 4.6 敌人意图系统

敌人 AI 不是完全随机，而是“候选动作 + 历史约束 + 权重”。

```ts
type EnemyMove = {
  id: string;
  intent: IntentIcon;
  baseDamage?: number;
  hits?: number;
  block?: number;
  weight: number;
  minTurn?: number;
  cooldown?: number;
  maxConsecutive?: number;
  effects: EffectDef[];
};
```

生成逻辑：

```text
读取敌人当前状态
  ↓
过滤不可用动作
  ├── 冷却中
  ├── 连续次数超限
  ├── HP 阈值不满足
  ├── 特定阶段不满足
  └── 特殊剧情/机制限制
  ↓
按权重随机
  ↓
写入 nextMove
  ↓
UI 展示意图
```

意图系统的设计价值：

- 玩家能提前看到风险。
- 防御牌不再是盲出，而是响应敌方攻击。
- 敌人随机性被限制在可理解范围内。
- 游戏从“猜敌人”变成“规划资源”。

### 4.7 状态 Power/Buff/Debuff 系统

状态本质是挂在角色身上的规则修改器。

```ts
type PowerInstance = {
  id: string;
  ownerId: string;
  amount: number;
  duration?: number;
  hooks: PowerHook[];
};
```

常见 Hook：

```text
atStartOfTurn
atEndOfTurn
onUseCard
onPlayAttack
onPlaySkill
onGainBlock
onAttackToChangeDamage
onAttackedToChangeDamage
onLoseHp
onDeath
```

状态系统要支持叠层规则：

| 类型 | 示例 | 叠加方式 |
|---|---|---|
| 数值叠加 | 力量、敏捷、毒 | amount 相加 |
| 回合叠加 | 易伤、虚弱 | duration 相加或刷新 |
| 唯一存在 | 能力牌效果 | 同 ID 合并 |
| 特殊上限 | 无实体 | 可能有最大层数 |

---

## 5. 卡牌系统

### 5.1 卡牌的三个层次

```text
卡牌定义 CardDefinition
  静态配置：名字、费用、类型、效果、稀有度

卡牌实例 CardInstance
  玩家拥有的具体一张牌：uuid、是否升级、临时费用

卡牌运行状态 CardRuntimeState
  本场战斗中发生的变化：本回合费用、保留、虚无、复制来源
```

这样设计可以解决：

- 同一张牌多份拷贝。
- 临时生成牌。
- 复制牌。
- 升级牌。
- 本回合费用变化。
- 战斗结束后临时牌消失。

### 5.2 卡牌执行方式

可以采用“数据驱动 + 少量脚本扩展”的混合方式。

```json
{
  "id": "shrug_it_off",
  "type": "SKILL",
  "color": "RED",
  "rarity": "COMMON",
  "cost": 1,
  "target": "SELF",
  "effects": [
    { "kind": "block", "amount": 8 },
    { "kind": "draw", "amount": 1 }
  ],
  "upgrade": {
    "effects": [
      { "kind": "block", "amount": 11 },
      { "kind": "draw", "amount": 1 }
    ]
  }
}
```

复杂牌保留自定义类：

```ts
class LimitBreakCard extends CardScript {
  use(ctx: UseCardContext) {
    const strength = ctx.player.getPowerAmount("strength");
    ctx.queue.add(new ApplyPowerAction(ctx.player, "strength", strength));
    if (!this.instance.upgraded) {
      ctx.queue.add(new ExhaustCardAction(this.instance));
    }
  }
}
```

### 5.3 费用系统

费用不只是 `cost: number`。

```ts
type CostDef =
  | { kind: "fixed"; value: number }
  | { kind: "xCost" }
  | { kind: "freeThisTurn" }
  | { kind: "unplayable" }
  | { kind: "special"; evaluatorId: string };
```

运行时需要区分：

| 字段 | 含义 |
|---|---|
| `baseCost` | 原始费用 |
| `costForTurn` | 本回合费用 |
| `costForCombat` | 本场战斗费用 |
| `isCostModified` | UI 是否显示变色 |

### 5.4 卡牌奖励池

战斗后通常给 3 选 1。实现上是从角色颜色池、无色池、特殊池中按规则抽取。

```text
生成奖励
  ↓
确定奖励来源：普通战斗/精英/Boss/事件
  ↓
确定颜色池：角色色 + 特殊条件
  ↓
按稀有度权重抽取
  ↓
排除不合法卡
  ↓
生成 3 张候选
  ↓
玩家选择 1 张或跳过
```

关键设计是“可以跳过”。跳过让卡组纯度成为资源，也让玩家不是每次奖励都必须变强。

### 5.5 卡牌平衡：标准值偏移

可以设一个基础价值模型，但不要机械套公式。

```text
1 费攻击基础线：约 6-9 点直接伤害
1 费防御基础线：约 5-8 点格挡
0 费牌：通常弱效果或有条件
2 费牌：需要显著高于两张 1 费牌的组合价值
能力牌：前期亏节奏，长期回本
消耗牌：允许单次强度更高
带负面牌：允许数值超标
带条件牌：条件越苛刻，收益越高
```

偏移方式：

| 偏移 | 例子 | 目的 |
|---|---|---|
| 加条件 | 需要特定手牌/状态 | 提高上限，降低稳定性 |
| 加负面 | 失血、加伤口、消耗 | 换取超额数值 |
| 加延迟 | 毒、成长、能力 | 牺牲当回合节奏 |
| 加协同标签 | 消耗、弃牌、姿态 | 进入流派网络 |

---

## 6. 角色系统

### 6.1 角色不是皮肤，而是规则入口

角色差异由四件事组成：

```text
初始牌组
初始遗物
专属卡池
专属机制
```

### 6.2 四个角色的设计差异

| 角色 | 初始 HP | 初始遗物 | 核心机制 | 典型流派 |
|---|---:|---|---|---|
| 铁甲战士 | 80 | 燃烧之血，战后回血 | 力量、消耗、失血、防御反打 | 力量流、壁垒流、消耗流 |
| 寂静猎手 | 70 | 蛇之戒指，开局多抽 | 毒、弃牌、刀刃、过牌 | 毒流、弃牌流、小刀流 |
| 故障机器人 | 75 | 破损核心，开局闪电球 | 充能球、集中、能量、能力 | 冰霜球、闪电球、黑暗球、能力流 |
| 观者 | 72 | 纯净之水，开局奇迹 | 姿态、保留、预见、爆发 | 愤怒爆发、平静循环、神格 |

### 6.3 故障机器人的充能球系统

充能球是一个队列系统：

```text
Orb slots: [slot0] [slot1] [slot2]

生成新球：
  如果有空位 → 放入最右空位
  如果满了 → 激发最左球 → 所有球左移 → 新球进入最右

每回合结束：
  所有球触发被动效果

主动激发：
  最左球触发主动效果并移除
```

数据模型：

```ts
type OrbInstance = {
  id: "lightning" | "frost" | "dark" | "plasma";
  passiveAmount: number;
  evokeAmount: number;
  misc?: number;
};
```

集中（Focus）是对充能球效果的全局修正器。它使机器人形成独立于“攻击/技能牌”的第三种战斗资源。

### 6.4 观者的姿态系统

姿态可以看作互斥状态机：

```text
无姿态
  ↔ 平静 Calm
  ↔ 愤怒 Wrath
  ↔ 神格 Divinity
```

设计作用：

- 愤怒：输出翻倍，但受到攻击风险极高。
- 平静：退出时获得能量，服务循环。
- 神格：短期爆发，通常需要计数/积累。

实现上姿态适合做成唯一 Power：

```ts
function enterStance(player, nextStance) {
  const prev = player.stance;
  triggerHooks("onExitStance", prev);
  player.stance = nextStance;
  triggerHooks("onEnterStance", nextStance);
}
```

---

## 7. 地图与关卡生成

### 7.1 地图的本质

地图不是简单关卡列表，而是有向无环图：

```ts
type MapGraph = {
  act: number;
  rows: MapNode[][];
  edges: MapEdge[];
  bossId: string;
};

type MapNode = {
  id: string;
  row: number;
  col: number;
  roomType: RoomType;
  outgoing: string[];
  incoming: string[];
};
```

玩家每次选择的是未来风险曲线：

```text
多精英路线：高风险高收益
多问号路线：不确定性更高
多商店路线：需要金币支撑
多休息路线：容错更高但成长可能不足
```

### 7.2 生成流程

```text
1. 用种子初始化 RNG
2. 创建固定行数和列数的网格
3. 生成多条从底部到顶部的路径
4. 避免路径交叉造成视觉和选择混乱
5. 为节点分配房间类型
6. 应用约束规则
7. 放置 Boss 和 Boss 前休息点
```

房间约束示例：

```text
开局前几层通常不出现精英
Boss 前固定休息
宝箱层有固定节奏点
商店不宜连续出现
同类安全节点不宜过度堆叠
路径需要至少保留多个有效选择
```

### 7.3 地图生成伪代码

```ts
function generateMap(seed: number, act: number): MapGraph {
  const rng = new Rng(seed);
  const grid = createEmptyGrid(15, 7);
  const pathCount = rng.int(5, 7);

  for (let p = 0; p < pathCount; p++) {
    let col = rng.int(0, 6);
    for (let row = 0; row < 15; row++) {
      const node = ensureNode(grid, row, col);
      const nextCol = clamp(col + rng.choice([-1, 0, 1]), 0, 6);
      connect(node, ensureNode(grid, row + 1, nextCol));
      col = nextCol;
    }
  }

  assignRoomTypes(grid, rng, act);
  enforceMapRules(grid, rng);
  return buildGraph(grid, act);
}
```

重点是“可复现”。同一个 seed 必须生成同一张地图，否则复盘、调试、每日挑战、排行榜都会出问题。

---

## 8. 奖励、经济与成长

### 8.1 奖励结构

普通战斗奖励一般由这些部分构成：

```text
金币
卡牌 3 选 1
药水概率掉落
特殊条件下的遗物/事件奖励
```

精英和宝箱提供遗物，Boss 提供 Boss 遗物和进入下一幕。

### 8.2 遗物系统

遗物是全局被动规则修改器。

```ts
type RelicDefinition = {
  id: string;
  rarity: RelicRarity;
  triggerHooks: HookName[];
  counters?: CounterDef[];
  scriptId?: string;
};
```

遗物常见触发方式：

| 触发点 | 示例用途 |
|---|---|
| 战斗开始 | 加力量、生成卡、加能量 |
| 回合开始 | 抽牌、获得格挡 |
| 打出某类牌 | 计数、增益 |
| 受伤时 | 反伤、减伤 |
| 进入房间 | 获得金币、改变奖励 |
| 商店 | 折扣、移除牌费用变化 |
| 休息处 | 新增选项 |

遗物设计的重点是“不占用出牌资源”，所以它们能显著改变卡组评价。

### 8.3 药水系统

药水是一次性战术保险。

```text
默认栏位有限
获取概率动态变化
使用后消失
可在任意合适时机打出
```

实现上药水类似 0 费、一次性、不进牌堆的卡：

```ts
type PotionDefinition = {
  id: string;
  rarity: Rarity;
  target: TargetType;
  effects: EffectDef[];
  usablePhase: CombatPhase[];
};
```

动态掉落概率的意义是减少极端随机：

```text
连续不掉 → 概率上升
刚掉过 → 概率下降
```

### 8.4 商店系统

商店本质是“用金币购买确定性”。

可售内容：

```text
卡牌
遗物
药水
删牌服务
```

商店平衡点：

- 删除基础牌非常强，因为它提升抽牌质量。
- 买遗物是长期收益。
- 买药水是短期保险。
- 买卡牌是补齐构筑缺口。

### 8.5 休息处系统

休息处一般提供二选一：

```text
回血：提高短期生存
升级：提高长期战力
```

这个系统看似简单，但它把 HP 转换成战力：

```text
当前 HP 足够 → 可以升级 → 未来更强
当前 HP 不足 → 被迫回血 → 成长变慢
```

所以前面战斗中少掉血，本质上等于获得了一次升级机会。

---

## 9. 事件系统

### 9.1 事件是可控随机

事件不能只是随机奖励，它要提供交易：

```text
付出 HP → 获得遗物/删牌/升级
获得诅咒 → 获得金币/遗物
失去金币 → 获得卡牌/移除牌
进入战斗 → 获得高额奖励
```

事件结构：

```ts
type EventDefinition = {
  id: string;
  act: ActFilter;
  weight: number;
  conditions: ConditionDef[];
  screens: EventScreenDef[];
};

type EventOption = {
  textKey: string;
  condition?: ConditionDef;
  effects: RunEffectDef[];
  nextScreen?: string;
};
```

### 9.2 事件池分层

事件按幕和条件分池：

```text
通用事件池
第一幕事件池：低风险，帮助启动
第二幕事件池：中风险，考验构筑
第三幕事件池：高风险，强化终局
特殊事件池：角色、遗物、路线条件触发
```

这样能保证事件奖励和玩家当前阶段匹配。

---

## 10. 数值平衡与指标驱动

### 10.1 为什么这类游戏必须指标驱动

卡牌构筑游戏的组合爆炸非常严重：

```text
角色 × 卡牌 × 遗物 × 药水 × 敌人 × 地图 × 事件 × 进阶等级
```

靠人工直觉很难判断单张牌强弱，因为一张牌可能：

- 单独很弱，配合很强。
- 低进阶强，高进阶弱。
- 第一幕强，第三幕弱。
- 对某个 Boss 特别强。
- 在高手手里强，新手手里弱。

### 10.2 应采集的指标

```text
Run 级：
  角色、进阶等级、胜负、死亡楼层、Boss、总时长

Deck 级：
  最终卡组、删牌数、升级数、卡牌获取楼层

Card 级：
  选取率、跳过率、胜率、平均获取楼层、升级率

Relic 级：
  获取率、获取后胜率、平均获取楼层

Combat 级：
  敌人、回合数、失血、药水使用、死亡原因

Map 级：
  路线、精英数量、商店访问、休息选择
```

### 10.3 平衡方法

不要只看胜率，要交叉看：

```text
高选取 + 高胜率 = 可能过强
低选取 + 高胜率 = 可能是高手牌/特定流派牌
高选取 + 低胜率 = 可能是新手陷阱
低选取 + 低胜率 = 可能需要增强或重做
```

还要分阶段看：

```text
第一幕强牌：帮助启动，后期可弱
后期强牌：前期不能太拖节奏
Boss 解法牌：平时可一般，但关键时有价值
流派核心牌：需要足够可见和足够稀有
```

---

## 11. 底层技术架构

### 11.1 《杀戮尖塔 1》：Java + libGDX

公开资料显示，《杀戮尖塔 1》使用 Java 和 libGDX 开发。libGDX 是 Java 游戏开发框架，不是 Unity/Godot 那种完整编辑器引擎。

典型技术分层可理解为：

```text
Game Application
  ├── Run / Combat / Card / Relic / Event 业务系统
  ├── UI 与输入处理
  ├── 资源管理：纹理、字体、音频、本地化
  ├── 存档和设置
  └── 平台服务：Steam 成就、统计、云存档

libGDX
  ├── ApplicationListener 游戏循环
  ├── SpriteBatch 2D 渲染
  ├── TextureAtlas 资源图集
  ├── Audio 音频
  ├── Input 输入
  └── LWJGL/OpenGL 桌面后端

JVM / Native Backend
  └── Windows / macOS / Linux
```

libGDX 的特点：

| 方面 | 影响 |
|---|---|
| 轻量框架 | 可控性强，但工具链弱 |
| Java/JVM | 迭代快，跨桌面平台方便 |
| 无强编辑器 | UI、动画、数据管线需要自建 |
| 字节码易分析 | 间接促进 Mod 生态 |
| OpenGL/LWJGL | 2D 卡牌游戏性能足够 |

### 11.2 游戏循环

libGDX 常见主循环：

```java
public class GameApp extends ApplicationAdapter {
    public void create() {
        loadAssets();
        initGameState();
    }

    public void render() {
        float dt = Gdx.graphics.getDeltaTime();
        update(dt);
        draw();
    }

    public void dispose() {
        disposeAssets();
    }
}
```

对于《杀戮尖塔》这种游戏，核心不是物理模拟，而是：

```text
输入 → 状态机 → 行动队列 → 动画队列 → 渲染
```

### 11.3 渲染层

2D 卡牌游戏通常使用：

```text
SpriteBatch 绘制背景、角色、卡牌、图标
TextureAtlas 管理小图
BitmapFont / 自定义字体渲染文字
粒子系统表现攻击、火焰、毒等特效
插值动画表现卡牌移动、缩放、旋转、淡入淡出
```

卡牌渲染可以拆成：

```text
卡框
费用宝石
插画
标题
描述文本
类型条
稀有度/颜色视觉
动态数值高亮
```

卡牌描述系统需要支持动态文本：

```text
造成 !D! 点伤害。获得 !B! 点格挡。
```

其中 `!D!`、`!B!` 在运行时由力量、敏捷、易伤等修正后显示。

### 11.4 输入层

输入系统重点处理：

```text
卡牌 hover
卡牌拖拽
目标选择
确认出牌
结束回合
地图节点选择
奖励选择
商店购买
事件选项
```

卡牌拖拽是状态机：

```text
IDLE
  ↓ 鼠标按下卡牌
DRAGGING_CARD
  ↓ 拖到目标区域
TARGETING
  ↓ 松开并目标合法
PLAY_CARD
  ↓
IDLE
```

### 11.5 存档系统

存档需要覆盖：

```text
当前 RunState
解锁进度
角色进阶等级
统计数据
设置项
每日挑战/排行榜缓存
```

为保证可复现，存档里必须保存：

```text
seed
各系统 RNG 状态
当前地图
当前房间状态
卡牌实例 UUID 和升级状态
遗物计数器
事件 flags
```

### 11.6 《杀戮尖塔 2》：Godot + C#

截至 2026-05-30，官方 FAQ 显示《杀戮尖塔 2》使用 Godot，并已于 2026-03-05 进入 Steam Early Access。官方 Press Kit 也提到新引擎、视觉、QoL 和更扩展的 Mod 能力。

迁移到 Godot 的工程含义：

| 方面 | 变化 |
|---|---|
| 引擎层 | 从 libGDX 框架转向完整开源引擎 |
| 语言层 | 公开资料和社区逆向显示核心逻辑大量使用 C#/.NET |
| 工具层 | 场景、资源导入、编辑器、动画和 UI 工具有更完整支持 |
| Mod 层 | 官方强调 expanded moddability，具体 API 仍随 Early Access 迭代 |
| 跨平台 | Godot 原生支持桌面和多平台导出 |

Godot/C# 适合的结构：

```text
Godot Scene
  ├── CombatScene
  ├── MapScene
  ├── ShopScene
  ├── EventScene
  └── RewardScene

C# Domain Layer
  ├── RunState
  ├── CombatState
  ├── CardSystem
  ├── RelicSystem
  ├── EnemyAI
  ├── RewardGenerator
  └── SaveSystem
```

理想做法是让核心战斗逻辑尽量脱离 Godot Node：

```text
纯 C# 规则层：可测试、可回放、可模拟
Godot 表现层：动画、UI、音效、输入
```

这样可以支持自动化平衡测试和 Mod 扩展。

### 11.7 Mod 技术

《杀戮尖塔 1》的 Mod 生态主要围绕：

```text
ModTheSpire：外部 Mod 加载器
BaseMod：常用 Mod API 和开发基础
StSLib：扩展机制库
Steam Workshop：分发渠道
```

ModTheSpire 的关键点是“不修改原始游戏文件加载外部 Mod”。Java 字节码和反射/补丁技术让社区可以扩展卡牌、角色、遗物、事件、UI 和机制。

典型 Mod 内容：

```text
新角色
新卡池
新遗物
新药水
新事件
新 Boss
平衡调整
自动化/AI 控制接口
```

这说明原游戏的系统边界足够清楚：卡牌、遗物、角色、事件、敌人都是可注册的内容类型。

---

## 12. 如果复刻一个类似游戏，推荐模块划分

### 12.1 模块结构

```text
src/
├── core/
│   ├── rng/
│   ├── state/
│   ├── actions/
│   └── hooks/
├── content/
│   ├── cards/
│   ├── relics/
│   ├── potions/
│   ├── enemies/
│   ├── events/
│   └── characters/
├── combat/
│   ├── combat-state.ts
│   ├── turn-system.ts
│   ├── card-play-system.ts
│   ├── damage-system.ts
│   ├── draw-system.ts
│   └── enemy-ai.ts
├── run/
│   ├── run-state.ts
│   ├── map-generator.ts
│   ├── reward-generator.ts
│   ├── shop-system.ts
│   └── event-system.ts
├── presentation/
│   ├── scenes/
│   ├── ui/
│   ├── animation/
│   └── audio/
└── tools/
    ├── content-validator
    ├── balance-simulator
    └── localization-exporter
```

### 12.2 最小可玩版本 MVP

不要一开始做所有内容。最小闭环：

```text
1 个角色
20 张卡
8 个普通敌人
3 个精英
3 个 Boss
20 个遗物
5 个药水
1 幕地图
商店、休息、宝箱、奖励
```

优先实现顺序：

```text
1. 战斗状态机
2. 卡牌定义与效果系统
3. 抽弃牌循环
4. 敌人意图
5. 奖励选择
6. 地图路线
7. 遗物 Hook
8. 商店/休息/事件
9. 存档和统计
10. 内容扩展与平衡工具
```

### 12.3 容易踩坑的地方

| 问题 | 后果 | 建议 |
|---|---|---|
| 卡牌逻辑全部硬编码 | 后期无法扩内容 | 数据驱动 + 脚本扩展 |
| 没有行动队列 | 触发顺序混乱 | 所有效果进队列 |
| UI 和规则强耦合 | 难测试、难模拟 | 规则层纯逻辑 |
| RNG 不分流 | 改一个随机会影响全局 | 地图/战斗/奖励 RNG 分开 |
| 无内容校验 | 配表错误运行时才炸 | 写 content validator |
| 无战斗日志 | Bug 难复现 | 保存行动日志 |
| 无指标 | 平衡靠感觉 | 采集选取率/胜率/死亡楼层 |
| 过早做大量卡 | 系统问题被内容掩盖 | 先做小卡池验证闭环 |

---

## 13. 《杀戮尖塔》的关键设计公式

### 13.1 决策压力公式

```text
决策压力 = 当前威胁 × 资源稀缺 × 未来不确定性
```

敌人意图提供当前威胁，能量/手牌/HP 提供资源稀缺，地图和奖励提供未来不确定性。

### 13.2 构筑价值公式

```text
一张牌的价值 =
  当前战斗即时价值
  + 未来战斗成长价值
  + 与已有卡/遗物协同价值
  - 污染牌组的抽牌成本
  - 使用条件成本
```

这就是为什么“拿牌”不是永远正确。

### 13.3 路线价值公式

```text
路线价值 =
  预期奖励
  - 预期 HP 损耗
  + 商店/休息/事件提供的选择权
  + 对当前卡组短板的补偿
```

高水平玩家不是只看哪个节点奖励高，而是看当前卡组是否能支付这条路线的风险。

---

## 14. 参考资料

- [Mega Crit FAQ](https://www.megacrit.com/faq/)：官方 FAQ，包含《杀戮尖塔 2》Early Access 时间和 Godot 信息。
- [Slay the Spire 2 Press Kit](https://www.megacrit.com/press-kits/slay-the-spire-2/)：官方 Press Kit，包含发布日期、平台、新引擎、扩展 Mod 能力等信息。
- [Slay the Spire 2 Early Access Launch](https://megacrit.com/news/2026-03-05-early-access-launch/)：官方 Early Access 上线公告。
- [Road to the IGF: Mega Crit Games' Slay the Spire](https://www.gamedeveloper.com/game-platforms/road-to-the-igf-mega-crit-games-i-slay-the-spire-i-)：Game Developer 对 Mega Crit 的采访，提到 libGDX 和设计挑战。
- [Seattle Indies Spotlight: Mega Crit Games](https://www.seattleindies.org/seattle-indies-spotlight-mega-crit-games/)：采访中提到 LWJGL/OpenGL 层、libGDX 优缺点和数据化测试。
- [ModTheSpire GitHub](https://github.com/kiooeht/ModTheSpire)：Slay the Spire 外部 Mod 加载器。
- [BaseMod GitHub](https://github.com/daviscook477/BaseMod)：Slay the Spire 常用 Mod API 和开发控制台。
- [Slay the Spire Steam Page](https://store.steampowered.com/app/646570/Slay_the_Spire/)：游戏标签、平台特性、创意工坊等公开信息。
