# 第 1 轮 · DESIGN · 杀戮尖塔 Like 网页核心循环

文档类型：DESIGN 设计规范
目标产品：跑在网页里的类杀戮尖塔卡牌爬塔游戏
版本范围：V1 单人网页游戏 MVP
主目标：桌面端可玩，重点兼容 1366x768；移动端不崩即可
视觉方向：暗色策略卡牌桌面、清晰克制、带尖塔探索感

## 0. 设计原则

1. 新局默认进入 Map，玩家选择第 1 个可达敌人节点后进入 Battle；不做营销页、落地页、Hero 区。
2. Battle 是 V1 最高频、最重要的主工作界面；所有战斗核心信息在 1366x768 一屏内可见：玩家资源、敌人、手牌、牌堆、回合按钮、敌人意图。
3. 暗色桌面不是紫蓝单色渐变；主色使用炭黑、旧铜、血红、羊皮纸、幽绿和少量冷青。
4. 不做卡片套卡片。页面区块可以有边框和面板，但卡牌、奖励卡、商店商品不能再嵌套装饰卡。
5. 视觉优先服务策略判断：数值、意图、费用、可打出状态必须比装饰更醒目。
6. V1 使用清晰的 2D UI，不要求复杂 3D 或粒子堆叠，但世界观视觉素材必须由 Image2 生成并裁切成游戏素材使用。

## 0A. Image2 视觉资产规范

### 0A.1 硬口径

所有世界观素材必须来自 Image2 生成图，并经过裁切、透明化或压缩后作为游戏资产使用，不允许用占位符、纯色块、emoji、文字标签或临时几何图形替代。

lucide 或等价线性图标、文字兜底只允许用于纯 UI 功能按钮和系统操作，例如设置、关闭、确认、返回、排序、筛选；不可替代背景、玩家角色、敌人、Boss、物品、遗物、金币、宝箱、路线节点图标或卡牌插画。

### 0A.2 必备素材清单

V1 至少准备以下 Image2 资产：

| 类别 | 资产 | 数量建议 | 用途 |
|---|---|---:|---|
| 背景 | Battle 背景 | 1-2 | 战斗桌面和敌人区域铺底 |
| 背景 | Map 背景 | 1 | 路线地图画布底图 |
| 背景 | Shop 背景 | 1 | 商店主体氛围图 |
| 背景 | Rest 背景 | 1 | 休息点/篝火界面 |
| 背景 | Event 背景 | 2-3 | 事件页左侧或整页氛围图 |
| 角色 | 玩家角色 | 1 | Top Bar 头像、Battle 玩家区域 |
| 敌人 | 普通敌人 | 3-5 | 普通战斗节点池 |
| 敌人 | 精英 | 2 | 精英战斗节点 |
| 敌人 | Boss | 1 | Boss 战与地图 Boss 节点 |
| 物品 | 遗物 | 8-12 | 遗物栏、奖励、商店 |
| 物品 | 金币 | 1 | 金币栏、奖励飞入动效 |
| 物品 | 宝箱 | 1 | 宝箱节点、奖励来源 |
| 地图 | 路线节点图标 | 6 类 | 普通战斗、精英、事件、商店、休息、宝箱、Boss |
| 卡牌 | 卡牌插画 | 每张卡 1 张 | 卡牌 art-box、奖励卡、商店卡牌商品 |

### 0A.3 规格与导出

- 背景原图建议 `1920x1080`，网页展示裁切到容器，保留主要视觉焦点；Battle 背景需预留下方手牌区和左右 HUD 暗化空间。
- 玩家、普通敌人、精英、Boss 建议透明背景 PNG；源图建议 `1024x1024`，裁切后角色主体占画面 `78%-88%`，四周留边 `6%-10%`，避免动画抖动时被裁掉。
- 遗物、金币、宝箱、路线节点图标建议透明背景 PNG；源图建议 `512x512`，2x 导出可覆盖 `32x32`、`44x44`、`64x64`、`96x96` 等 UI 尺寸。
- 卡牌插画建议 `512x320` 或 `768x480` 横图，裁切到卡牌 art-box 时保持主体可识别；不得用纯色纹理、文字或符号替代插画。
- 背景优先导出 WebP，透明素材优先导出 PNG；如 PNG 体积过大，可另存 WebP 预览版，但透明边缘不能出现明显脏边。
- 所有素材按 2x 清晰度导出，界面中用 CSS 压到目标尺寸，避免高 DPI 屏发糊。
- 命名规则：`scene-battle-01.webp`、`scene-map-01.webp`、`char-player-01.png`、`enemy-normal-slime-01.png`、`enemy-elite-guard-01.png`、`enemy-boss-spire-eye-01.png`、`relic-blood-vial-01.png`、`item-gold-01.png`、`node-shop-01.png`、`card-strike-01.webp`。
- 目录建议：`public/assets/scenes/`、`public/assets/characters/`、`public/assets/enemies/`、`public/assets/relics/`、`public/assets/items/`、`public/assets/nodes/`、`public/assets/cards/`。
- 同一批素材必须保持一致的暗色奇幻策略卡牌风格：低饱和、旧铜高光、羊皮纸局部暖色、边缘略旧化；禁止同屏混用像素风、Q 版、写实摄影、二次元赛璐璐等不一致风格。

### 0A.4 UI 使用方式

- Battle 背景铺满 `combat-board`，使用暗色蒙层保证血条、意图和伤害数字可读；敌人素材站在底座上，普通敌人宽 `120-160px`，精英宽 `150-190px`，Boss 宽 `220-280px`。
- 玩家角色显示在 Left HUD 头像和 Battle 玩家区域；头像使用同一角色图裁切，不另用文字头像。
- Map 背景铺满 `map-canvas`；路线节点图标使用 Image2 裁切素材，节点状态只叠加描边、透明度和发光，不替换为文字或 emoji。
- Shop/Rest/Event 背景作为界面氛围底图或左侧图像区，叠加面板时必须保留主体可见，不用纯色块充当场景。
- 卡牌 `art-box` 必须渲染对应卡牌插画；奖励卡和商店卡牌商品复用同一张卡牌插画。
- 遗物栏、奖励遗物、商店遗物商品必须使用遗物素材；金币奖励和金币栏必须使用金币素材；宝箱节点和宝箱奖励必须使用宝箱素材。

## 1. 配色 Token

### 1.1 基础背景

| Token | 色值 | 用途 |
|---|---|---|
| `--bg-page` | `#11100E` | 整页底色，接近炭黑 |
| `--bg-table` | `#181613` | 战斗桌面主背景 |
| `--bg-panel` | `#211E19` | HUD 面板、商店、奖励、事件主体 |
| `--bg-panel-2` | `#2A251E` | 二级信息条、列表行 |
| `--bg-inset` | `#0B0A09` | 牌堆计数、能量槽、血量内嵌底 |
| `--bg-hover` | `#332D24` | 可交互区域 hover |
| `--bg-disabled` | `#171615` | 禁用按钮/禁用卡牌 |

### 1.2 文字与边框

| Token | 色值 | 用途 |
|---|---|---|
| `--text-main` | `#F2E8D8` | 主文字 |
| `--text-sub` | `#C7B79D` | 次级文字 |
| `--text-muted` | `#8E826F` | 弱提示、说明 |
| `--text-inverse` | `#17120D` | 亮色按钮文字 |
| `--border-soft` | `#443B2E` | 普通面板边框 |
| `--border-strong` | `#6E5B3F` | 高亮边框 |
| `--border-focus` | `#D6A84B` | 当前选择、可确认状态 |

### 1.3 状态与资源

| Token | 色值 | 用途 |
|---|---|---|
| `--hp` | `#B83A32` | 玩家/敌人生命 |
| `--hp-dark` | `#5B1D19` | 血条底色 |
| `--block` | `#6E9FB7` | 格挡 |
| `--energy` | `#E0A638` | 能量 |
| `--gold` | `#D8A33E` | 金币、奖励强调 |
| `--poison` | `#74A34A` | 中毒/持续伤害 |
| `--power` | `#8D6FD1` | 能力/增益，仅少量使用 |
| `--danger` | `#D14B3F` | 攻击意图、危险操作 |
| `--success` | `#6EA66F` | 获得奖励、确认 |
| `--warning` | `#D28A36` | 风险提示、精英节点 |
| `--info` | `#6CA8B8` | 抽牌、牌堆、路线可达 |

### 1.4 卡牌类型

| Token | 色值 | 用途 |
|---|---|---|
| `--card-attack` | `#7B2F2A` | 攻击牌标题栏 |
| `--card-skill` | `#2F5E70` | 技能牌标题栏 |
| `--card-power` | `#5C4A8E` | 能力牌标题栏 |
| `--card-status` | `#5D5A52` | 状态牌标题栏 |
| `--card-curse` | `#322238` | 诅咒牌标题栏 |
| `--card-body` | `#E7D7B6` | 卡牌正文底 |
| `--card-border` | `#5A4630` | 卡牌默认描边 |
| `--card-selected` | `#F0B84E` | 手牌选中描边 |
| `--card-unplayable` | `#4A4740` | 费用不足/目标不合法蒙层 |

### 1.5 阴影与发光

| Token | 值 | 用途 |
|---|---|---|
| `--shadow-panel` | `0 8px 24px rgba(0,0,0,.35)` | 大面板 |
| `--shadow-card` | `0 6px 14px rgba(0,0,0,.42)` | 手牌/奖励卡 |
| `--shadow-pop` | `0 10px 28px rgba(0,0,0,.55)` | 弹窗、奖励浮层 |
| `--glow-focus` | `0 0 0 2px #D6A84B, 0 0 18px rgba(214,168,75,.35)` | 当前可确认对象 |
| `--glow-danger` | `0 0 18px rgba(209,75,63,.38)` | 即将受伤/敌人攻击 |
| `--glow-block` | `0 0 16px rgba(110,159,183,.32)` | 格挡获得 |

## 2. 字体规则

### 2.1 字体栈

```css
--font-ui: Inter, "PingFang SC", "Microsoft YaHei", system-ui, sans-serif;
--font-title: "Cinzel", "Noto Serif SC", "Songti SC", serif;
--font-number: "DIN Alternate", "Barlow Condensed", Arial, sans-serif;
```

实现时不强制引入外部字体。若无网络字体，使用本地字体栈兜底。

### 2.2 字级

| 层级 | 尺寸/行高 | 用途 |
|---|---|---|
| `display` | `32px / 38px` | GameOver 标题、Boss 名称，不在普通面板滥用 |
| `h1` | `24px / 30px` | 当前界面主标题 |
| `h2` | `20px / 26px` | 奖励、商店、事件标题 |
| `h3` | `16px / 22px` | 小节标题、卡牌名 |
| `body` | `14px / 20px` | 常规说明 |
| `small` | `12px / 16px` | 标签、状态说明 |
| `tiny` | `10px / 14px` | 牌堆计数、稀有度标签 |
| `number-lg` | `28px / 32px` | HP、能量、金币等核心数值 |
| `number-md` | `18px / 22px` | 伤害、格挡、费用 |

### 2.3 字体硬规则

1. 不使用视口宽度缩放字体。
2. 字间距保持 `0`，不使用负字距。
3. 按钮文字最多 6 个中文字符，超过时换成图标加 tooltip 或缩短文案。
4. 卡牌正文最多 3 行，超出用更短文案，不允许字号小于 `11px`。
5. 战斗核心数字使用 `--font-number`，避免和叙述文本混在一起。

## 3. 间距、圆角、边框

| Token | 值 | 用途 |
|---|---:|---|
| `--space-1` | `4px` | 图标与数字间隔 |
| `--space-2` | `8px` | 组件内边距、按钮间距 |
| `--space-3` | `12px` | 面板内标准间距 |
| `--space-4` | `16px` | 区块间距 |
| `--space-5` | `24px` | 大区块间距 |
| `--radius-sm` | `4px` | 标签、计数器 |
| `--radius-md` | `6px` | 按钮、小面板 |
| `--radius-lg` | `8px` | 大面板、卡牌 |
| `--border-1` | `1px solid var(--border-soft)` | 默认边框 |
| `--border-2` | `2px solid var(--border-strong)` | 强边框 |

V1 所有卡片和面板圆角不得超过 `8px`。

## 4. 整页布局

### 4.1 全局框架

桌面端固定为游戏应用布局：

```text
body 100vw x 100vh
└── .game-shell
    ├── .top-bar      高 48px
    ├── .main-stage   flex-1
    └── .toast-layer  fixed overlay
```

`.game-shell` 要求：

- `width: 100vw; height: 100vh; overflow: hidden;`
- 背景为 `--bg-page`，中间战斗桌面使用 `--bg-table`。
- 1366x768 下不得出现页面级纵向滚动；只有商店商品列表、事件文本长段落允许内部滚动。

### 4.2 Top Bar

高度 `48px`，横向排列：

```text
[角色头像/名称] [Floor 7 / Act 1] [金币] [遗物缩略栏] [设置]
```

规格：

- 左侧角色区宽 `180px`。
- 地图进度区宽 `140px`。
- 金币区宽 `88px`。
- 遗物栏 `flex:1`，单个遗物 `32x32`，最多显示 12 个，超出折叠为 `+N`。
- 设置按钮 `36x32`，只用齿轮图标，hover 显示“设置”。
- 药水系统为 V2 预留，V1 Top Bar 不显示可操作药水栏，也不显示空药水槽。

## 5. 七类界面布局

### 5.1 Battle 战斗界面

Battle 是 V1 最高频、最重要的主工作界面，但不是新局默认首屏。新局先进入 Map，玩家点击第 1 个可达敌人节点后进入 Battle。

```text
main-stage
├── left-hud        260px
├── combat-board    flex-1
└── right-hud       220px
```

#### Left HUD

从上到下：

1. 玩家状态：头像、名称、HP、格挡。
2. 能量：3 个大能量珠，显示 `当前/上限`。
3. 抽牌堆/弃牌堆/消耗堆：三枚堆叠图标，带数量。
4. 当前战斗日志：最多 4 行，旧消息淡出。

尺寸：

- `left-hud width: 260px; padding: 12px;`
- 面板间距 `12px`。
- HP 条高 `18px`，格挡徽章 `40x40`。
- 能量珠 `42x42`，间距 `8px`。

#### Combat Board

垂直三段：

```text
enemy-zone    42%
intent-strip  8%
hand-zone     50%
```

敌人区：

- 单敌居中，多敌横向排列，最多 3 个。
- 敌人站位底座宽 `160-220px`，Boss 可到 `260px`。
- 敌人上方显示名称、血条、状态图标。
- 意图图标位于敌人头顶右上，不遮挡血条。

手牌区：

- 手牌位于底部，横向扇形排列。
- 1366 宽下显示 10 张仍不重叠到不可读；超出 10 张进入压缩间距，不换行。
- 手牌上方留出出牌拖拽轨道，高 `96px`。
- 当前可选目标用 `--glow-focus` 描边。

#### Right HUD

从上到下：

1. 回合控制：结束回合主按钮。
2. 当前敌人意图汇总：总伤害、减益、特殊行动。
3. 玩家状态列表：力量、敏捷、易伤、虚弱等。
4. 当前房间目标：击败所有敌人。

规格：

- `right-hud width: 220px; padding: 12px;`
- 结束回合按钮宽 `100%`，高 `48px`。
- 意图汇总每行高 `28px`。

### 5.2 Map 地图界面

Map 是新局默认首屏，也是战斗、奖励、商店、休息、事件之间的宏观路由界面。地图不是全屏营销页，而是替换 `main-stage` 内容，保留 Top Bar。

布局：

```text
map-view
├── map-header  当前 Act / Floor / 路线提示
├── map-canvas  路线节点图
└── map-footer  当前节点说明 / 返回不可用提示
```

规格：

- `map-canvas` 占据可用高度 `calc(100vh - 140px)`。
- 节点列按楼层从下到上排列，最少 8 层，最多 15 层。
- 新局进入 Map 时，底部第 1 层至少有 1 个可达敌人节点。
- 玩家必须先选择第 1 个可达敌人节点，才进入第一场 Battle。
- 节点大小 `44x44`，Boss 节点 `64x64`。
- 可达节点使用 `--border-focus` 和轻微呼吸动画。
- 已走路径为旧铜色实线，未走路径为 `#3A3328` 虚线。
- 不可达节点透明度 `.35`，不可点击。

### 5.3 Reward 奖励界面

奖励界面在战斗胜利后出现，使用居中面板，不遮住 Top Bar。

布局：

```text
reward-view
├── title: 战斗胜利
├── reward-row: 金币 / 遗物
├── card-choice: 3 张奖励卡
└── actions: 跳过 / 确认
```

规格：

- 主面板宽 `880px`，最大高 `calc(100vh - 96px)`。
- 奖励卡 `156x220`，间距 `18px`。
- 选中卡牌抬升 `-10px`，描边 `--card-selected`。
- 跳过按钮为次级按钮，确认按钮为主按钮。
- 无卡牌奖励时 `card-choice` 区隐藏，不留大空洞。

### 5.4 Shop 商店界面

商店保留资源栏，主体分三列：

```text
shop-view
├── left: 卡牌商品 3-5 个
├── center: 遗物商品
└── right: 服务区 删除牌 / 离开
```

规格：

- 主体内边距 `20px`。
- 商品卡牌复用奖励卡规格，但增加价格标签。
- 遗物商品为 `96x116` 商品块，不再包一层卡。
- 已购买商品显示售罄斜章，透明度 `.45`，不可点击。
- 删除牌服务固定在右列顶部，价格大数字显示。
- 离开按钮固定右列底部，高 `48px`。

### 5.5 Rest 休息界面

休息界面强调二选一决策。

布局：

```text
rest-view
├── campfire-visual  中央篝火/休息点视觉
├── choices          休息 / 升级
└── deck-preview     选择升级时显示可升级卡牌
```

规格：

- 休息与升级按钮各 `240x120`，并排。
- 休息显示预计回复数值，例如 `+22 HP`。
- 升级选择后进入卡组选择层，卡牌网格 `5列 x N行`，内部滚动。
- 已升级卡牌角标显示 `UPGRADED`，不可选。

### 5.6 Event 事件界面

事件界面是阅读加选择，不做小说页。

布局：

```text
event-view
├── event-art      左侧 38%
└── event-panel    右侧 62%
    ├── title
    ├── body
    └── choices
```

规格：

- 事件正文最大 8 行，超过内部滚动。
- 选择按钮纵向排列，每个高 `44px`，间距 `10px`。
- 代价用红色前缀，例如 `失去 8 HP`。
- 收益用金色/绿色前缀，例如 `获得 75 金币`。
- 不满足条件的选项保留显示但禁用，说明原因。

### 5.7 GameOver 结算界面

GameOver 分死亡和通关两种视觉状态。

布局：

```text
gameover-view
├── result-title
├── run-summary: 楼层 / 击杀 / 遗物 / 卡组数量
├── final-deck-preview
└── actions: 再来一局 / 返回地图概览
```

规格：

- 死亡标题使用 `--danger`，通关标题使用 `--gold`。
- 总结数字使用 `number-lg`。
- 卡组预览为横向小卡列表，内部滚动。
- `再来一局` 为主按钮，点击后创建新局并进入 Map；`返回地图概览` 为次级按钮。

## 6. 核心组件样式规范

### 6.1 卡牌

规格：

- 手牌尺寸：`132x184`。
- 奖励/商店尺寸：`156x220`。
- 小预览尺寸：`82x116`。
- 圆角 `8px`，边框 `2px solid var(--card-border)`。
- 卡牌正文背景 `--card-body`，文字 `#21170F`。

结构：

```text
card
├── cost-badge      左上 30x30
├── title-bar       高 30px，按类型着色
├── art-box         高 68px，使用对应 Image2 卡牌插画裁切图
├── rules-text      2-3 行效果文本
└── type-rarity     底部标签
```

状态：

- 可打出：正常亮度，hover 上移 `8px`。
- 选中：上移 `16px`，`--glow-focus`。
- 费用不足：灰色蒙层 `.55`，费用徽章变 `--danger`。
- 无合法目标：灰色蒙层 `.45`，hover 不抬升。
- 升级牌：标题右侧显示 `+`，边框轻微金色。

### 6.2 敌人意图

意图图标尺寸 `44x44`，放在敌人上方。意图可以使用 UI 图标表达行为类型，但不能替代敌人本体素材。

| 意图 | 颜色 | 显示 |
|---|---|---|
| 攻击 | `--danger` | 剑图标 + 伤害数字，例如 `8` |
| 多段攻击 | `--danger` | 双剑 + `5x3` |
| 防御 | `--block` | 盾图标 + 格挡值 |
| 强化 | `--power` | 上箭头/符文 |
| 减益 | `--poison` | 下箭头/毒滴 |
| 未知 | `--text-muted` | 问号，仅特殊敌人使用 |

硬规则：

- 攻击总伤害必须在 Right HUD 汇总。
- 多敌人时每个敌人的意图不能只依赖颜色，必须有图标和数字。
- 敌人行动后，新意图生成要有 `220ms` 翻面动画。

### 6.3 血条

玩家血条：

- 宽 `100%`，高 `18px`。
- 文案居中 `当前/最大`。
- 低于 30% 时血条闪烁边框，周期 `900ms`。

敌人血条：

- 宽 `150px`，高 `14px`。
- 多敌人保持同一宽度。
- Boss 血条宽 `220px`。

格挡：

- 玩家格挡显示为血条右侧盾牌徽章。
- 敌人格挡显示在血条下方小盾牌。
- 回合开始清零时使用 `160ms` 向下淡出。

### 6.4 能量

能量为玩家每回合行动预算。

规格：

- 能量珠 `42x42`，圆形。
- 已有能量：填充 `--energy`，内阴影。
- 已消耗能量：填充 `--bg-inset`，边框 `--border-soft`。
- 数字显示在能量区右侧：`2/3`。

动画：

- 回合开始恢复能量：每颗间隔 `80ms` 点亮。
- 出牌消耗：对应数量能量珠 `scale(.86)` 后暗下，持续 `140ms`。

### 6.5 牌堆

三类牌堆固定在 Left HUD。牌堆图标属于 UI 功能图标，可使用线性图标；牌堆弹窗中的卡牌预览仍必须显示卡牌插画。

| 牌堆 | 图标 | 文案 |
|---|---|---|
| 抽牌堆 | 牌背 | `抽牌 12` |
| 弃牌堆 | 倾斜牌 | `弃牌 8` |
| 消耗堆 | 灰烬 | `消耗 1` |

规格：

- 单个牌堆按钮 `72x58`。
- hover 打开 tooltip，列出最多 8 张牌名，超出显示 `+N`。
- 点击牌堆打开查看弹窗；弹窗宽 `720px`，卡牌小预览网格。

### 6.6 路线节点

节点尺寸：

- 普通战斗 `44x44`
- 精英 `48x48`
- 商店/休息/事件 `46x46`
- Boss `64x64`

节点类型。节点图标必须使用 Image2 裁切素材，颜色只用于状态叠加：

| 类型 | 图标 | 主色 |
|---|---|---|
| 普通战斗 | 剑 | `--text-sub` |
| 精英 | 角盔 | `--warning` |
| 事件 | 问号 | `--info` |
| 商店 | 钱袋 | `--gold` |
| 休息 | 火焰 | `--success` |
| 宝箱 | 宝箱 | `--gold` |
| Boss | 尖塔眼 | `--danger` |

状态：

- 可达：`--glow-focus`，cursor pointer。
- 已完成：图标变小，节点底色旧铜。
- 不可达：透明度 `.35`，不可点击。
- 当前节点：外圈旋转描边，周期 `1600ms`。

### 6.7 奖励卡

奖励卡复用卡牌规格 `156x220`，增加：

- 稀有度角标：普通/罕见/稀有。
- 选择态：上移 `10px`，金色描边。
- hover：显示升级预览差异，tooltip 不遮挡确认按钮。
- 跳过奖励时，三张卡整体淡出 `180ms`。

### 6.8 遗物栏

Top Bar 中横向展示。

规格：

- 遗物图标 `32x32`，使用 Image2 遗物透明素材，不使用文字、emoji 或线性图标替代。
- hover tooltip 宽 `260px`，显示名称、效果、获得来源。
- 新获得遗物从奖励区飞入遗物栏，`420ms`。
- 遗物触发时图标上移 `4px` 并亮金边 `280ms`。

### 6.9 V2 药水预留

V1 不做药水系统，不在 Battle、Reward、Shop、Top Bar 中显示可操作药水栏。

预留约束：

- 数据层可以保留 `potions` 字段或扩展位，但 V1 UI 不渲染。
- 不显示空槽、不显示瓶形图标、不提供使用药水交互。
- V2 若启用，建议放回 Top Bar 设置按钮左侧，3 槽，每槽 `34x34`。
- V2 若启用，必须补充药水 tooltip、目标选择、战斗外禁用态和奖励/商店来源规范。

### 6.10 按钮

基础样式：

- 圆角 `6px`。
- 高度：主按钮 `48px`，普通按钮 `40px`，小按钮 `32px`。
- 边框 `1px solid rgba(255,255,255,.12)`。
- 阴影 `0 3px 0 rgba(0,0,0,.45)`。
- hover：亮度 `1.08`，上移 `-1px`。
- active：上移 `1px`，阴影减半。
- disabled：透明度 `.45`，无 hover/active。

按钮类型：

| 类型 | 背景 | 用途 |
|---|---|---|
| Primary | `--gold` | 确认、继续、再来一局 |
| Combat | `--danger` | 结束回合、攻击确认 |
| Safe | `--success` | 获得、休息、治疗 |
| Secondary | `--bg-panel-2` | 跳过、返回 |
| Ghost | transparent | 查看、排序、小工具 |
| Danger | `--danger` | 放弃、删除确认、危险事件选项 |

硬规则：

- 结束回合按钮始终在 Right HUD 顶部，不能移动到底部手牌旁。
- 主行动按钮每个界面最多 1 个。
- 危险操作必须二次确认，或按钮文案明确代价。

## 7. 动画时序

全局规则：

- 动画使用 transform 和 opacity，避免布局抖动。
- 普通反馈控制在 `80-300ms`。
- 跨区飞行动画控制在 `320-520ms`。
- 同一时刻最多 2 类强动效，避免信息噪声。
- 用户快速操作时，关键状态以状态机为准，不依赖动画结束才更新数据。

### 7.1 发牌

```text
0ms      抽牌堆图标轻震
80ms     第 1 张牌从抽牌堆飞入手牌槽
160ms    第 2 张牌飞入
240ms+   后续每张间隔 80ms
结束后   手牌执行 180ms 扇形重排
```

要求：

- 补牌期间可禁用出牌，避免手牌位置未稳定时点错。
- 若抽牌堆不足，先播放弃牌堆洗回抽牌堆 `300ms`，再继续发牌。

### 7.2 出牌

```text
0ms      玩家点击/拖拽卡牌到目标
80ms     卡牌从手牌区飞向敌人或玩家区
220ms    卡牌效果结算，目标闪光
320ms    卡牌进入弃牌堆/消耗堆
420ms    手牌重排
```

攻击牌：

- 飞向目标敌人中心偏下。
- 命中时敌人向后抖动 `120ms`。

技能牌：

- 飞向玩家区域。
- 格挡/抽牌等效果以 HUD 数值跳动表现。

能力牌：

- 飞向玩家状态列表。
- 状态图标新增时亮边 `300ms`。

### 7.3 伤害

```text
0ms      命中闪光
40ms     伤害数字从目标位置弹出
80ms     血条扣减开始
220ms    血条扣减完成
360ms    伤害数字淡出上移
```

规格：

- 普通伤害数字 `24px`，暴击/高伤害 `32px`。
- 被格挡吸收时优先扣格挡，数字颜色为 `--block`。
- 破盾时播放盾牌碎裂 `180ms`。

### 7.4 格挡

```text
0ms      盾牌徽章出现或更新
80ms     格挡数字滚动到新值
180ms    盾牌外圈发光结束
```

回合开始格挡清零：

- 盾牌向下淡出 `160ms`。
- 若有保留格挡效果，显示小标签 `保留`，不播放清零动画。

### 7.5 敌人意图

生成意图：

```text
0ms      旧意图翻面
120ms    显示新图标
220ms    Right HUD 汇总更新
```

敌人行动：

```text
0ms      当前行动敌人底座亮起
160ms    敌人前冲或施法动作
260ms    伤害/减益/防御结算
420ms    敌人回到站位
```

多敌人按从左到右行动，每个敌人之间间隔 `180ms`。

### 7.6 奖励选择

```text
0ms      胜利标题淡入
180ms    金币/遗物奖励逐个出现
360ms    三张奖励卡从下方上浮
选择时   被选卡上移 10px，其余透明度 .65
确认后   选卡飞入卡组摘要或淡出
```

若跳过：

- 三张卡同时淡出 `180ms`。
- 跳过按钮显示 `已跳过` `300ms` 后进入地图。

### 7.7 地图节点切换

```text
0ms      点击可达节点
80ms     路线线段点亮
220ms    当前节点外圈移动到目标节点
420ms    地图淡出，进入房间界面
```

进入 Boss 节点时额外：

- 背景压暗 `180ms`。
- Boss 节点红色脉冲 `2` 次，每次 `260ms`。

## 8. 文案锁定

| 场景 | 文案 |
|---|---|
| 结束回合 | `结束回合` |
| 能量 | `能量` |
| 抽牌堆 | `抽牌` |
| 弃牌堆 | `弃牌` |
| 消耗堆 | `消耗` |
| 金币 | `金币` |
| 遗物 | `遗物` |
| 战斗胜利 | `战斗胜利` |
| 选择奖励 | `选择 1 张牌加入牌组` |
| 跳过奖励 | `跳过奖励` |
| 继续 | `继续` |
| 地图标题 | `选择下一处房间` |
| 商店离开 | `离开商店` |
| 删除牌 | `删除一张牌` |
| 休息 | `休息` |
| 升级 | `升级` |
| 事件继续 | `继续前进` |
| 死亡标题 | `攀登失败` |
| 通关标题 | `抵达塔顶` |
| 再来一局 | `再来一局` |

文案规则：

- 不在界面中解释“这是一个类杀戮尖塔游戏”。
- 不显示教程式大段说明。
- 事件选项文案必须同时写清收益或代价。

## 9. AI 实施者硬约束

1. 不要把 V1 做成首页、宣传页或角色选择展示页；新局默认进入 Map，并要求玩家选择第 1 个可达敌人节点后进入 Battle。
2. Battle 是最高频主工作界面，但不能跳过 Map 节点选择直接开始第一场战斗。
3. 不要使用紫蓝单色渐变作为主视觉；紫色只允许用于能力牌和少量状态。
4. 不要卡片套卡片。奖励卡、商店商品、事件选择都是单层组件。
5. 1366x768 必须一屏可玩；Battle 不允许页面级滚动。
6. 结束回合按钮固定在 Right HUD 顶部，不能和手牌按钮混排。
7. 敌人意图必须同时出现在敌人上方和 Right HUD 汇总。
8. 卡牌费用不足、目标非法、禁用按钮必须有明确视觉状态。
9. 手牌最多 10 张时仍要可读；不得因为 hover/选中造成布局跳动。
10. 动画不能阻塞状态结算；状态更新和动画播放要解耦。
11. 所有核心颜色必须来自本文 token，不要在组件里散落随机 hex。
12. 移动端只要求不崩：可改为横向提示或压缩布局，但不要为移动端牺牲桌面主体验。
13. 不接入真实付费、账号、排行榜、联网对战、UGC、复杂存档云同步。
14. V1 不要求像素风，不要求复刻原作 UI，不使用原作素材。
15. 世界观素材必须由 Image2 生成并裁切使用；不得用占位符、纯色块、emoji、文字、lucide 或线性图标替代背景、角色、敌人、Boss、物品、遗物、金币、宝箱、路线节点图标或卡牌插画。
16. lucide 或等价线性图标、文字兜底只允许用于纯 UI 功能按钮和系统操作，不可承担游戏世界观素材职责。
17. 卡牌、遗物必须保留 tooltip 入口，不能只靠名称猜效果。
18. 药水系统为 V2 预留，V1 不实现可见药水栏、药水奖励、药水商品、药水使用按钮或目标选择。

## 10. 视觉验收清单

### Battle

- [ ] Battle 只能由 Map 节点选择进入；新局不能默认直接进入 Battle。
- [ ] 1366x768 下玩家 HP、能量、牌堆、敌人、敌人意图、手牌、结束回合按钮全部可见。
- [ ] Battle 背景、玩家角色、敌人、精英/Boss、卡牌插画均使用 Image2 生成并裁切后的素材。
- [ ] 手牌 10 张时不遮挡结束回合按钮和敌人区。
- [ ] 费用不足的卡牌一眼能看出不可打出。
- [ ] 敌人多段攻击显示为 `NxM`，不是只显示总数。
- [ ] 玩家即将承受总伤害能在 Right HUD 读到。

### Map

- [ ] 新局默认进入 Map，而不是 Battle、首页或角色选择页。
- [ ] 第 1 层至少 1 个敌人节点可达，点击后进入第一场 Battle。
- [ ] Map 背景和路线节点图标均使用 Image2 生成并裁切后的素材。
- [ ] 可达、已走、不可达节点状态区分明确。
- [ ] 路线从下往上推进，当前所在节点明确。
- [ ] Boss 节点在视觉上明显高于普通节点。

### Reward

- [ ] 三张奖励卡尺寸一致，选中态明确。
- [ ] 奖励卡、遗物、金币、宝箱均使用 Image2 素材，不用占位图或文字替代。
- [ ] 跳过奖励不比选择卡牌更醒目。
- [ ] 获得遗物/金币时能看出进入了哪个资源栏。
- [ ] V1 奖励界面不出现药水奖励、药水槽或药水使用入口。

### Shop

- [ ] 商品、价格、买不起、已购买四种状态清晰。
- [ ] 商店背景、卡牌商品图、遗物商品图均使用 Image2 素材。
- [ ] 删除牌服务固定可见。
- [ ] 离开商店按钮固定在右下区域，不被商品列表挤走。
- [ ] V1 商店不出现药水商品或药水栏。

### Rest

- [ ] 休息和升级是平级选择。
- [ ] Rest 背景和篝火/休息点视觉使用 Image2 素材，不用纯色面板替代。
- [ ] 升级卡牌时能看出升级前后差异。
- [ ] 已升级卡牌不可重复升级。

### Event

- [ ] 事件正文不会把选项挤出屏幕。
- [ ] Event 背景或左侧事件图使用 Image2 素材，不用空白、纯色块或文字替代。
- [ ] 每个选项的代价/收益可读。
- [ ] 禁用选项说明原因。

### GameOver

- [ ] 死亡和通关状态视觉不同。
- [ ] 本局摘要至少包含楼层、金币、遗物数、牌组数。
- [ ] 再来一局是最醒目的操作。

### 全局视觉

- [ ] 页面没有营销 Hero。
- [ ] 页面没有紫蓝单色渐变主背景。
- [ ] 没有卡片套卡片。
- [ ] V1 任意界面都没有可操作药水栏；药水仅作为 V2 隐藏预留存在于设计说明中。
- [ ] 圆角不超过 `8px`。
- [ ] 所有按钮文字不溢出。
- [ ] 所有核心数值更新都有轻量反馈。
- [ ] 动效不会造成布局抖动。
- [ ] 任意世界观视觉位都没有占位符、纯色块、emoji、文字标签、lucide 或线性图标替代素材。

## 11. 关键假设

1. V1 默认使用一个角色、一套初始牌组、一幕地图；设计规格保留扩展到多角色和多幕的空间。
2. V1 不追求大规模内容量，但所有背景、物品、人物/玩家角色、敌人/Boss、路线节点图标和卡牌插画都必须来自 Image2 生成并裁切后的游戏素材。
3. V1 地图、奖励、商店、事件、休息都服务于核心循环验证，不追求内容量。
4. V1 桌面端优先，设计基准为 1366x768；更大屏幕只增加留白和组件间距，不改变信息层级。
5. 设计规范面向 AI 实施者直接落地，因此优先锁定尺寸、状态、颜色、动效时序，而不是产出品牌叙事。
